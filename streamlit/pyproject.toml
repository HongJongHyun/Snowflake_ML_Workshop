[project]
name = "snow-cvs-forecast-dashboard"
version = "1.0.0"
description = "Snow편의점 매출 예측 대시보드"
dependencies = [
    "streamlit>=1.48.0",
    "altair",
    "pandas",
]

[tool.snowflake.project]
definition_version = "2"

[tool.snowflake.project.streamlit]
main_file = "streamlit_app.py"
query_warehouse = "ML_WORKSHOP_WH"
